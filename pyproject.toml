[project]
name = "dungen"
version = "0.1.0"
description = "A text-based world builder, roleplay engine, and story generator."
readme = "README.md"
authors = [
    { name = "Alejandro Piad", email = "apiad@apiad.net" }
]
requires-python = ">=3.12"
dependencies = [
    "lingo-ai>=0.4.0",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
    "pyyaml>=6.0.3",
    "typer>=0.21.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
dungen = "dungen.cli:app"

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
